export default class User {

    constructor(id, firstName, surName, city, age) {
        this.firstName = firstName
        this.surName = surName
        this.id = id
        this.city = city
        this.age=age
    }
}
